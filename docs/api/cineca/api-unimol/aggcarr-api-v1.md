---
title: Aggiorna Carriere API V1 | OhMyUniversity!
description: REST API documentation for the Aggiorna Carriere service (aggcarr-service-v1) - CINECA ESSE3.
head:
  - - meta
    - property: og:title
      content: Aggiorna Carriere API V1 | OhMyUniversity!
  - - meta
    - property: og:description
      content: REST API documentation for the Aggiorna Carriere service (aggcarr-service-v1) - CINECA ESSE3.
  - - meta
    - property: og:url
      content: https://docs.university.ohmyopensource.org/api/cineca/api-unimol/aggcarr-api-v1
  - - meta
    - name: keywords
      content: aggcarr api, career update api, aggiorna carriere, esse3 rest api, cineca api, ohmyuniversity api, academic record update, student career management
  - - meta
    - name: twitter:title
      content: Aggiorna Carriere API V1 | OhMyUniversity!
  - - meta
    - name: twitter:description
      content: REST API documentation for the Aggiorna Carriere service (aggcarr-service-v1) - CINECA ESSE3.
---

# OhMyUniversity! - Unimol: Aggiorna Carriere API V1

**ENG:** `Academic Record Update`

**Version:** `1.0.0` · **Base URL:** `/aggcarr-service-v1`

Engine for updating student academic records, both in bulk (by department or degree) and individually (by student ID).

---

## How it works

Each procedure runs in two modes depending on the endpoint used:

- **`/aggcarr/{nomeProc}`** - bulk mode, targets multiple students based on filters
- **`/aggcarr/{nomeProc}/{matId}`** - single mode, targets one specific student

### Available procedures

- `AGG_AD_OFF` - Syncs teaching activity attributes from the reference offer
- `FREQ` - Assigns attendance frequency
- `RIMUOVI_FREQ` - Removes attendance frequency
- `SEG` - Updates activity segments in the student record from the reference offer
- `SOST` - Substitutes one teaching activity with another

---

## Common filters

All procedures accept a `filtriComuni` object to scope which students are affected.

- `facId` / `facCod` - department/faculty (use one)
- `cdsId` / `cdsCod` - degree course (use one)
- `aaOrdId` - academic year of the curriculum (requires `cdsId` or `cdsCod`)
- `pdsId` / `pdsCod` - study plan, relative to `cdsId` and `aaOrdId` (use one)
- `aaRegId` - cohort year

::: warning
Bulk calls require at least `facId` or `cdsId`. Single calls only need `matId` - common filters are optional.
:::

---

## Execution flow

### Bulk

1. Call `/aggcarr/{nomeProc}` - response includes a `link` header pointing to the preview
2. Review affected rows via `/aggcarr/{aggcarrId}/dettagli`
3. Optionally exclude rows via `/aggcarr/{aggcarrId}/{aggcarrdettId}/elabora`
4. Trigger execution via `/aggcarr/{aggcarrId}/{aggcarrdettId}/esegui`
5. Check results via `/aggcarr/{aggcarrId}/log`
6. Delete the elaboration when done

### Single

1. Call `/aggcarr/{nomeProc}/{matId}` - log is returned directly
2. Review results via `/aggcarr/{aggcarrId}/dettagli`
3. Delete the elaboration when done

---

## Endpoints - Careers Update (Aggiorna Carriere)

### `GET /aggcarr` - Get all career update headers

```java
/**
 * Returns the list of career update process headers (testate).
 * Each entry represents a bulk update session with its associated
 * degree course, faculty, study plan, and runtime parameters.
 *
 * @param none
 * @return List<TestataAggiornaCarriera> list of career update headers,
 *         or an empty array if none are available
 */
GET /aggcarr
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Response

**`200 OK`**

```json
[
  {
    "aggcarrId": 123, // Career update header ID (primary key)
    "procCod": "string", // Process code
    "stato": 0, // Process status
    "facId": 1, // Faculty ID
    "facCod": "fac_cod", // Faculty code
    "facDes": "fac_des", // Faculty description
    "cdsId": 1, // Degree course ID
    "cdsCod": "cds_cod", // Degree course code
    "cdsDes": "cds_des", // Degree course description
    "aaOrdId": 1, // Curriculum academic year ID
    "pdsId": 1, // Study plan ID
    "pdsCod": "pds_cod", // Study plan code
    "pdsDes": "pds_des", // Study plan description
    "aaRegId": 1, // Cohort academic year ID
    "parametri": [
      {
        "aggcarrId": 123, // Career update header ID (primary key)
        "paramCod": "string", // Parameter code (primary key)
        "paramDes": "string", // Parameter description
        "valAlfa": "S1", // Alphanumeric value
        "valNum": 1 // Numeric value
      }
    ]
  }
]
```

<br>

---

<br>

### `GET /aggcarr/{aggcarrId}` - Get career update header by ID

```java
/**
 * Returns the header information of a specific career update preview session,
 * identified by the ID generated by one of the setup procedures.
 *
 * @param aggcarrId long (path, required) - ID generated by a setup procedure
 *                  that defines the list of entities to process
 * @return TestataAggiornaCarriera the career update header, or 404 if not found
 */
GET /aggcarr/{aggcarrId}
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Response

**`200 OK`**

```json
{
  "aggcarrId": 123, // Career update header ID (primary key)
  "procCod": "string", // Process code
  "stato": 0, // Process status
  "facId": 1, // Faculty ID
  "facCod": "fac_cod", // Faculty code
  "facDes": "fac_des", // Faculty description
  "cdsId": 1, // Degree course ID
  "cdsCod": "cds_cod", // Degree course code
  "cdsDes": "cds_des", // Degree course description
  "aaOrdId": 1, // Curriculum academic year ID
  "pdsId": 1, // Study plan ID
  "pdsCod": "pds_cod", // Study plan code
  "pdsDes": "pds_des", // Study plan description
  "aaRegId": 1, // Cohort academic year ID
  "parametri": [
    {
      "aggcarrId": 123, // Career update header ID (primary key)
      "paramCod": "string", // Parameter code (primary key)
      "paramDes": "string", // Parameter description
      "valAlfa": "S1", // Alphanumeric value
      "valNum": 1 // Numeric value
    }
  ]
}
```

<br>

---

<br>

### `DELETE /aggcarr/{aggcarrId}` - Delete career update preview

```java
/**
 * Deletes a career update preview session after the engine has been executed
 * and the results have been reviewed. Must be called to clean up the resource
 * at the end of both bulk and single invocation flows.
 *
 * @param aggcarrId long (path, required) - ID generated by a setup procedure
 *                  that defines the list of entities to process
 * @return 204 No Content on success
 */
DELETE /aggcarr/{aggcarrId}
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Response

**`204 No Content`** - Preview successfully deleted.

<br>

---

<br>

### `GET /aggcarr/{aggcarrId}/dettagli` - Get preview row list

```java
/**
 * Returns the paginated list of rows in a career update preview session.
 * Optional fields (adCod, adDes, aaFreqId, dataFreq, dataSup, profCod)
 * are excluded by default and must be explicitly requested via the
 * fields or optionalFields query parameters.
 *
 * @param aggcarrId      long (path, required)    - ID of the career update session
 * @param fields         string (query, optional) - comma-separated list of optional
 *                                                  fields to include; use ALL to
 *                                                  return all fields; supports
 *                                                  Ant Glob Patterns (e.g. obj.*, obj.**)
 * @param optionalFields string (query, optional) - alias for fields, same behavior
 * @param start          int (query, optional)    - index of the first record to load,
 *                                                  defaults to 0
 * @param limit          int (query, optional)    - number of records to retrieve
 *                                                  starting from start, defaults to 50,
 *                                                  allowed range: 0–100
 * @return List<RigaAggiornaCarriera> paginated list of preview rows
 */
GET /aggcarr/{aggcarrId}/dettagli
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Response

**`200 OK`**

```json
[
  {
    "aggcarrId": 123, // Career update header ID
    "aggcarrdettId": 123, // Preview row ID
    "stuId": 123, // Student career ID
    "matId": 123, // Student career segment ID
    "adsceId": 1233, // Student record row ID
    "adregId": 1233, // Student record entry ID
    "matricola": "matcod", // Student registration number
    "nome": "nome", // Student first name
    "cognome": "cognome", // Student last name
    "coorte": 2012, // Student cohort year
    "cdsStuId": 12345, // Enrolled degree course ID
    "cdsStuCod": "adCod", // Enrolled degree course code
    "cdsStuDes": "cdsStu", // Enrolled degree course description
    "aaOrdStuId": 12345, // Curriculum academic year ID
    "pdsStuId": 12345, // Enrolled study plan ID
    "pdsStuCod": "pdsCod", // Enrolled study plan code
    "pdsStuDes": "pdsDes", // Enrolled study plan description
    "adId": 12345, // Teaching activity ID
    "adCod": "adCod", // Teaching activity code (optional)
    "adDes": "adDes", // Teaching activity description (optional)
    "aaFreqId": 2016, // Attendance academic year (optional)
    "dataFreq": "10/10/2016", // Attendance date DD/MM/YYYY (optional)
    "dataSup": "10/10/2016", // Completion date DD/MM/YYYY (optional)
    "esito": {
      "modValCod": "V", // Grading mode: V=grade, G=judgement, N=none
      "supEsaFlg": 1, // Pass flag (0/1)
      "voto": 0, // Grade (only if modValCod=V, integer for final exams)
      "lodeFlg": 1, // Honours flag (0/1, only if modValCod=V)
      "tipoGiudCod": "IDO", // Judgement code (only if modValCod=G)
      "tipoGiudDes": "Idoneo", // Judgement description (only if modValCod=G)
      "dataEsa": "15/10/2015", // Exam date DD/MM/YYYY
      "aaSupId": 2016 // Completion academic year
    },
    "profCod": "profCod", // Student profile code (optional)
    "elaboraFlg": 0, // Process flag: 1=to process, 0=skip
    "errMsg": "string" // Error message if row processing failed
  }
]
```

<br>

---

<br>

### `GET /aggcarr/{aggcarrId}/dettagli/{aggcarrDettId}` - Get single preview row

```java
/**
 * Returns a single row from a career update preview session,
 * identified by both the session ID and the row ID.
 * Optional fields (adCod, adDes, aaFreqId, dataFreq, dataSup, profCod)
 * are excluded by default and must be explicitly requested via the
 * fields or optionalFields query parameters.
 *
 * @param aggcarrId      long (path, required)    - ID of the career update session
 * @param aggcarrDettId  long (path, required)    - ID of the specific preview row
 * @param fields         string (query, optional) - comma-separated list of optional
 *                                                  fields to include; use ALL to
 *                                                  return all fields; supports
 *                                                  Ant Glob Patterns (e.g. obj.*, obj.**)
 * @param optionalFields string (query, optional) - alias for fields, same behavior
 * @return RigaAggiornaCarriera the requested preview row
 */
GET /aggcarr/{aggcarrId}/dettagli/{aggcarrDettId}
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Response

**`200 OK`**

```json
{
  "aggcarrId": 123, // Career update header ID
  "aggcarrdettId": 123, // Preview row ID
  "stuId": 123, // Student career ID
  "matId": 123, // Student career segment ID
  "adsceId": 1233, // Student record row ID
  "adregId": 1233, // Student record entry ID
  "matricola": "matcod", // Student registration number
  "nome": "nome", // Student first name
  "cognome": "cognome", // Student last name
  "coorte": 2012, // Student cohort year
  "cdsStuId": 12345, // Enrolled degree course ID
  "cdsStuCod": "adCod", // Enrolled degree course code
  "cdsStuDes": "cdsStu", // Enrolled degree course description
  "aaOrdStuId": 12345, // Curriculum academic year ID
  "pdsStuId": 12345, // Enrolled study plan ID
  "pdsStuCod": "pdsCod", // Enrolled study plan code
  "pdsStuDes": "pdsDes", // Enrolled study plan description
  "adId": 12345, // Teaching activity ID
  "adCod": "adCod", // Teaching activity code (optional)
  "adDes": "adDes", // Teaching activity description (optional)
  "aaFreqId": 2016, // Attendance academic year (optional)
  "dataFreq": "10/10/2016", // Attendance date DD/MM/YYYY (optional)
  "dataSup": "10/10/2016", // Completion date DD/MM/YYYY (optional)
  "esito": {
    "modValCod": "V", // Grading mode: V=grade, G=judgement, N=none
    "supEsaFlg": 1, // Pass flag (0/1)
    "voto": 0, // Grade (only if modValCod=V, integer for final exams)
    "lodeFlg": 1, // Honours flag (0/1, only if modValCod=V)
    "tipoGiudCod": "IDO", // Judgement code (only if modValCod=G)
    "tipoGiudDes": "Idoneo", // Judgement description (only if modValCod=G)
    "dataEsa": "15/10/2015", // Exam date DD/MM/YYYY
    "aaSupId": 2016 // Completion academic year
  },
  "profCod": "profCod", // Student profile code (optional)
  "elaboraFlg": 0, // Process flag: 1=to process, 0=skip
  "errMsg": "string" // Error message if row processing failed
}
```

<br>

---

<br>

### `PUT /aggcarr/{aggcarrId}/dettagli/{aggcarrDettId}/elabora` - Set process flag on a preview row

```java
/**
 * Sets the elaboraFlg flag on a specific preview row, determining whether
 * the career update engine will process it during execution.
 * Accepts application/x-www-form-urlencoded body.
 * Returns the updated row with its current state.
 *
 * @param aggcarrId      long (path, required)    - ID of the career update session
 * @param aggcarrDettId  long (path, required)    - ID of the specific preview row
 * @param fields         string (query, optional) - optional fields to include;
 *                                                  use ALL for all; supports
 *                                                  Ant Glob Patterns (e.g. obj.*, obj.**)
 * @param optionalFields string (query, optional) - alias for fields, same behavior
 * @param elabora        boolean (formData, required) - true=process this row,
 *                                                      false=skip this row
 * @return RigaAggiornaCarriera the updated preview row on success,
 *         DettaglioErrore on failure
 */
PUT /aggcarr/{aggcarrId}/dettagli/{aggcarrDettId}/elabora
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

**Content-Type:** `application/x-www-form-urlencoded`

#### Response

**`200 OK`** - Flag updated. Returns the updated preview row.

```json
{
  "aggcarrId": 123, // Career update header ID
  "aggcarrdettId": 123, // Preview row ID
  "stuId": 123, // Student career ID
  "matId": 123, // Student career segment ID
  "adsceId": 1233, // Student record row ID
  "adregId": 1233, // Student record entry ID
  "matricola": "matcod", // Student registration number
  "nome": "nome", // Student first name
  "cognome": "cognome", // Student last name
  "coorte": 2012, // Student cohort year
  "cdsStuId": 12345, // Enrolled degree course ID
  "cdsStuCod": "adCod", // Enrolled degree course code
  "cdsStuDes": "cdsStu", // Enrolled degree course description
  "aaOrdStuId": 12345, // Curriculum academic year ID
  "pdsStuId": 12345, // Enrolled study plan ID
  "pdsStuCod": "pdsCod", // Enrolled study plan code
  "pdsStuDes": "pdsDes", // Enrolled study plan description
  "adId": 12345, // Teaching activity ID
  "adCod": "adCod", // Teaching activity code (optional)
  "adDes": "adDes", // Teaching activity description (optional)
  "aaFreqId": 2016, // Attendance academic year (optional)
  "dataFreq": "10/10/2016", // Attendance date DD/MM/YYYY (optional)
  "dataSup": "10/10/2016", // Completion date DD/MM/YYYY (optional)
  "esito": {
    "modValCod": "V", // Grading mode: V=grade, G=judgement, N=none
    "supEsaFlg": 1, // Pass flag (0/1)
    "voto": 0, // Grade (only if modValCod=V, integer for final exams)
    "lodeFlg": 1, // Honours flag (0/1, only if modValCod=V)
    "tipoGiudCod": "IDO", // Judgement code (only if modValCod=G)
    "tipoGiudDes": "Idoneo", // Judgement description (only if modValCod=G)
    "dataEsa": "15/10/2015", // Exam date DD/MM/YYYY
    "aaSupId": 2016 // Completion academic year
  },
  "profCod": "profCod", // Student profile code (optional)
  "elaboraFlg": 0, // Process flag: 1=to process, 0=skip
  "errMsg": "string" // Error message if row processing failed
}
```

**`422 Unprocessable Entity`** - Flag update failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `PUT /aggcarr/{aggcarrId}/esegui` - Execute career update on preview

```java
/**
 * Triggers the career update engine on the preview session.
 * Only rows with elaboraFlg=1 will be processed.
 * Returns an execution log with the detail of all operations performed.
 * Each log entry may contain an errMsg field if the row processing failed.
 *
 * @param aggcarrId long (path, required) - ID of the career update session
 * @return List<LogAggiornaCarriera> execution log on success,
 *         DettaglioErrore on failure
 */
PUT /aggcarr/{aggcarrId}/esegui
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Response

**`200 OK`** - Execution successful. Returns the execution log.

```json
[
  {
    "aggcarrId": 123, // Career update header ID
    "logTId": 123, // Log entry ID
    "logDes": "string" // Log description
  }
]
```

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `GET /aggcarr/{aggcarrId}/log` - Get career update execution log

```java
/**
 * Returns the execution log of a career update session.
 * The log is also available after calling the esegui endpoint
 * and can be consulted before deleting the preview resource.
 *
 * @param aggcarrId long (path, required) - ID of the career update session
 * @return List<LogAggiornaCarriera> the execution log entries
 */
GET /aggcarr/{aggcarrId}/log
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Response

**`200 OK`**

```json
[
  {
    "aggcarrId": 123, // Career update header ID
    "logTId": 123, // Log entry ID
    "logDes": "string" // Log description
  }
]
```

<br>

---

<br>

## Endpoints - Procedure Execution (Lancio Procedure)

### `POST /aggcarr/AGG_AD_OFF` - Bulk execution: AGG_AD_OFF

```java
/**
 * Triggers a bulk update of teaching activity attributes from the reference
 * offer for all matching students. Segments are excluded from this update.
 * On success, check the link response header for the URL of the generated
 * preview resource.
 *
 * @param filtriComuni       object (body, optional)  - common filters to scope
 *                                                      target students; facId or
 *                                                      cdsId required for bulk calls
 * @param forzaADLibereFlg   boolean (body, required) - whether to include free
 *                                                      teaching activities
 * @param adIdDaAggiornare   long (body, optional)    - ID (*) of the activity to
 *                                                      update; if null, all records
 *                                                      are updated
 * @param adCodDaAggiornare  string (body, optional)  - code (*) of the activity to
 *                                                      update; if null, all records
 *                                                      are updated
 * @return 201 Created on success, DettaglioErrore on failure
 *
 * (*) adIdDaAggiornare and adCodDaAggiornare are mutually exclusive
 */
POST /aggcarr/AGG_AD_OFF
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "filtriComuni": {
    "facId": 10, // Faculty/department ID (mutually exclusive with cdsId)
    "facCod": "facCod", // Faculty/department code (mutually exclusive with cdsCod)
    "cdsId": 10, // Degree course ID (mutually exclusive with facId)
    "cdsCod": "cdsCod", // Degree course code (mutually exclusive with facCod)
    "aaOrdId": 2010, // Curriculum academic year (requires cdsId or cdsCod)
    "pdsId": 9999, // Study plan ID (requires cdsId and aaOrdId)
    "pdsCod": "cdsCod", // Study plan code (requires cdsCod and aaOrdId)
    "aaRegId": 10 // Cohort year
  },
  "forzaADLibereFlg": false, // Include free teaching activities (required)
  "adIdDaAggiornare": 100, // Activity ID to update, null = all (*)
  "adCodDaAggiornare": "adCod" // Activity code to update, null = all (*)
}
```

#### Response

**`201 Created`** - Procedure successfully triggered. Check the `link` response header for the preview URL.

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `PUT /aggcarr/AGG_AD_OFF/{matId}` - Single execution: AGG_AD_OFF

```java
/**
 * Triggers a single-student update of teaching activity attributes from the
 * reference offer. Segments are excluded from this update.
 * The engine runs synchronously and returns the execution log directly.
 * filtriComuni is not required for single invocations.
 *
 * @param matId              long (path, required)    - ID of the student career
 *                                                      segment to process
 * @param forzaADLibereFlg   boolean (body, required) - whether to include free
 *                                                      teaching activities
 * @param adIdDaAggiornare   long (body, optional)    - ID (*) of the activity to
 *                                                      update; if null, all records
 *                                                      are updated
 * @param adCodDaAggiornare  string (body, optional)  - code (*) of the activity to
 *                                                      update; if null, all records
 *                                                      are updated
 * @return List execution log on success,
 *         DettaglioErrore on failure
 *
 * (*) adIdDaAggiornare and adCodDaAggiornare are mutually exclusive
 */
PUT /aggcarr/AGG_AD_OFF/{matId}
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "forzaADLibereFlg": false, // Include free teaching activities (required)
  "adIdDaAggiornare": 100, // Activity ID to update, null = all (*)
  "adCodDaAggiornare": "adCod" // Activity code to update, null = all (*)
}
```

> `filtriComuni` is accepted but not required - `matId` in the path already scopes the execution to a single student.

#### Response

**`200 OK`** - Execution successful. Returns the execution log directly.

```json
[
  {
    "aggcarrId": 123, // Career update header ID
    "logTId": 123, // Log entry ID
    "logDes": "string" // Log description
  }
]
```

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `POST /aggcarr/FREQ` - Bulk execution: FREQ

```java
/**
 * Triggers a bulk assignment of attendance frequency to student record
 * activities that have automatic office frequency set.
 * On success, check the link response header for the URL of the generated
 * preview resource.
 *
 * @param filtriComuni     object (body, optional)  - common filters to scope
 *                                                    target students; facId or
 *                                                    cdsId required for bulk calls
 * @param forzaNonPreviste boolean (body, required) - whether to assign frequency
 *                                                    even to activities without
 *                                                    automatic office frequency
 * @param forzaStatoF      boolean (body, required) - whether to recalculate
 *                                                    frequency even if already set
 * @param forzaDataFreq    boolean (body, required) - whether to assign the
 *                                                    frequency date even if not
 *                                                    defined in offer/curriculum
 * @param forzaStudentiX   boolean (body, required) - whether to include students
 *                                                    with a terminated career
 * @param adId             long (body, optional)    - ID (*) of the activity to
 *                                                    update; if null, all records
 *                                                    are updated
 * @param adCod            string (body, optional)  - code (*) of the activity to
 *                                                    update; if null, all records
 *                                                    are updated
 * @param fatPartCod       string (body, optional)  - partition factor code for
 *                                                    filtering; must be paired
 *                                                    with domPartCod
 * @param domPartCod       string (body, optional)  - partition domain code for
 *                                                    filtering; must be paired
 *                                                    with fatPartCod
 * @param partCod          string (body, optional)  - semester code for filtering
 * @return 201 Created on success, DettaglioErrore on failure
 *
 * (*) adId and adCod are mutually exclusive
 */
POST /aggcarr/FREQ
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "filtriComuni": {
    "facId": 10, // Faculty/department ID (mutually exclusive with cdsId)
    "facCod": "facCod", // Faculty/department code (mutually exclusive with cdsCod)
    "cdsId": 10, // Degree course ID (mutually exclusive with facId)
    "cdsCod": "cdsCod", // Degree course code (mutually exclusive with facCod)
    "aaOrdId": 2010, // Curriculum academic year (requires cdsId or cdsCod)
    "pdsId": 9999, // Study plan ID (requires cdsId and aaOrdId)
    "pdsCod": "cdsCod", // Study plan code (requires cdsCod and aaOrdId)
    "aaRegId": 10 // Cohort year
  },
  "forzaNonPreviste": false, // Include activities without automatic frequency (required)
  "forzaStatoF": false, // Recalculate even if already attended (required)
  "forzaDataFreq": false, // Assign frequency date even if not in offer (required)
  "forzaStudentiX": false, // Include students with terminated career (required)
  "adId": 1, // Activity ID to update, null = all (*)
  "adCod": "adCod", // Activity code to update, null = all (*)
  "fatPartCod": "ALF", // Partition factor code (requires domPartCod)
  "domPartCod": "AK", // Partition domain code (requires fatPartCod)
  "partCod": "S1" // Semester code filter
}
```

#### Response

**`201 Created`** - Procedure successfully triggered. Check the `link` response header for the preview URL.

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `PUT /aggcarr/FREQ/{matId}` - Single execution: FREQ

```java
/**
 * Triggers a single-student assignment of attendance frequency to student
 * record activities that have automatic office frequency set.
 * The engine runs synchronously and returns the execution log directly.
 * filtriComuni is not required for single invocations.
 *
 * @param matId            long (path, required)    - ID of the student career
 *                                                    segment to process
 * @param forzaNonPreviste boolean (body, required) - whether to assign frequency
 *                                                    even to activities without
 *                                                    automatic office frequency
 * @param forzaStatoF      boolean (body, required) - whether to recalculate
 *                                                    frequency even if already set
 * @param forzaDataFreq    boolean (body, required) - whether to assign the
 *                                                    frequency date even if not
 *                                                    defined in offer/curriculum
 * @param forzaStudentiX   boolean (body, required) - whether to include students
 *                                                    with a terminated career
 * @param adId             long (body, optional)    - ID (*) of the activity to
 *                                                    update; if null, all records
 *                                                    are updated
 * @param adCod            string (body, optional)  - code (*) of the activity to
 *                                                    update; if null, all records
 *                                                    are updated
 * @param fatPartCod       string (body, optional)  - partition factor code for
 *                                                    filtering; must be paired
 *                                                    with domPartCod
 * @param domPartCod       string (body, optional)  - partition domain code for
 *                                                    filtering; must be paired
 *                                                    with fatPartCod
 * @param partCod          string (body, optional)  - semester code for filtering
 * @return List<LogAggiornaCarriera> execution log on success,
 *         DettaglioErrore on failure
 *
 * (*) adId and adCod are mutually exclusive
 */
PUT /aggcarr/FREQ/{matId}
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "forzaNonPreviste": false, // Include activities without automatic frequency (required)
  "forzaStatoF": false, // Recalculate even if already attended (required)
  "forzaDataFreq": false, // Assign frequency date even if not in offer (required)
  "forzaStudentiX": false, // Include students with terminated career (required)
  "adId": 1, // Activity ID to update, null = all (*)
  "adCod": "adCod", // Activity code to update, null = all (*)
  "fatPartCod": "ALF", // Partition factor code (requires domPartCod)
  "domPartCod": "AK", // Partition domain code (requires fatPartCod)
  "partCod": "S1" // Semester code filter
}
```

> `filtriComuni` is accepted but not required - `matId` in the path already scopes the execution to a single student.

#### Response

**`200 OK`** - Execution successful. Returns the execution log directly.

```json
[
  {
    "aggcarrId": 123, // Career update header ID
    "logTId": 123, // Log entry ID
    "logDes": "string" // Log description
  }
]
```

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `POST /aggcarr/RIMUOVI_FREQ` - Bulk execution: RIMUOVI_FREQ

```java
/**
 * Triggers a bulk removal of attendance frequency from student record
 * activities. Opposite operation of FREQ.
 * On success, check the link response header for the URL of the generated
 * preview resource.
 *
 * @param filtriComuni   object (body, optional)  - common filters to scope
 *                                                  target students; facId or
 *                                                  cdsId required for bulk calls
 * @param forzaStudentiX boolean (body, required) - whether to include students
 *                                                  with a terminated career
 * @param adId           long (body, optional)    - ID (*) of the activity to
 *                                                  update; if null, all records
 *                                                  are updated
 * @param adCod          string (body, optional)  - code (*) of the activity to
 *                                                  update; if null, all records
 *                                                  are updated
 * @param fatPartCod     string (body, optional)  - partition factor code for
 *                                                  filtering; must be paired
 *                                                  with domPartCod
 * @param domPartCod     string (body, optional)  - partition domain code for
 *                                                  filtering; must be paired
 *                                                  with fatPartCod
 * @param partCod        string (body, optional)  - semester code for filtering
 * @return 201 Created on success, DettaglioErrore on failure
 *
 * (*) adId and adCod are mutually exclusive
 */
POST /aggcarr/RIMUOVI_FREQ
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "filtriComuni": {
    "facId": 10, // Faculty/department ID (mutually exclusive with cdsId)
    "facCod": "facCod", // Faculty/department code (mutually exclusive with cdsCod)
    "cdsId": 10, // Degree course ID (mutually exclusive with facId)
    "cdsCod": "cdsCod", // Degree course code (mutually exclusive with facCod)
    "aaOrdId": 2010, // Curriculum academic year (requires cdsId or cdsCod)
    "pdsId": 9999, // Study plan ID (requires cdsId and aaOrdId)
    "pdsCod": "cdsCod", // Study plan code (requires cdsCod and aaOrdId)
    "aaRegId": 10 // Cohort year
  },
  "forzaStudentiX": false, // Include students with terminated career (required)
  "adId": 1, // Activity ID to update, null = all (*)
  "adCod": "adCod", // Activity code to update, null = all (*)
  "fatPartCod": "ALF", // Partition factor code (requires domPartCod)
  "domPartCod": "AK", // Partition domain code (requires fatPartCod)
  "partCod": "S1" // Semester code filter
}
```

#### Response

**`201 Created`** - Procedure successfully triggered. Check the `link` response header for the preview URL.

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `PUT /aggcarr/RIMUOVI_FREQ/{matId}` - Single execution: RIMUOVI_FREQ

```java
/**
 * Triggers a single-student removal of attendance frequency from student
 * record activities. Opposite operation of FREQ.
 * The engine runs synchronously and returns the execution log directly.
 * filtriComuni is not required for single invocations.
 *
 * @param matId          long (path, required)    - ID of the student career
 *                                                  segment to process
 * @param forzaStudentiX boolean (body, required) - whether to include students
 *                                                  with a terminated career
 * @param adId           long (body, optional)    - ID (*) of the activity to
 *                                                  update; if null, all records
 *                                                  are updated
 * @param adCod          string (body, optional)  - code (*) of the activity to
 *                                                  update; if null, all records
 *                                                  are updated
 * @param fatPartCod     string (body, optional)  - partition factor code for
 *                                                  filtering; must be paired
 *                                                  with domPartCod
 * @param domPartCod     string (body, optional)  - partition domain code for
 *                                                  filtering; must be paired
 *                                                  with fatPartCod
 * @param partCod        string (body, optional)  - semester code for filtering
 * @return List<LogAggiornaCarriera> execution log on success,
 *         DettaglioErrore on failure
 *
 * (*) adId and adCod are mutually exclusive
 */
PUT /aggcarr/RIMUOVI_FREQ/{matId}
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "forzaStudentiX": false, // Include students with terminated career (required)
  "adId": 1, // Activity ID to update, null = all (*)
  "adCod": "adCod", // Activity code to update, null = all (*)
  "fatPartCod": "ALF", // Partition factor code (requires domPartCod)
  "domPartCod": "AK", // Partition domain code (requires fatPartCod)
  "partCod": "S1" // Semester code filter
}
```

> `filtriComuni` is accepted but not required - `matId` in the path already scopes the execution to a single student.

#### Response

**`200 OK`** - Execution successful. Returns the execution log directly.

```json
[
  {
    "aggcarrId": 123, // Career update header ID
    "logTId": 123, // Log entry ID
    "logDes": "string" // Log description
  }
]
```

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `POST /aggcarr/SEG` - Bulk execution: SEG

```java
/**
 * Triggers a bulk update of activity segments for student record rows,
 * syncing them from the reference offer.
 * On success, check the link response header for the URL of the generated
 * preview resource.
 *
 * @param filtriComuni   object (body, optional)  - common filters to scope
 *                                                  target students; facId or
 *                                                  cdsId required for bulk calls
 * @param forzaADLibereFlg boolean (body, required) - whether to recalculate
 *                                                    segments for free activities
 * @param forzaStatoFS   boolean (body, required) - whether to recalculate
 *                                                  segments even if the activity
 *                                                  is already attended or passed
 * @param forzaStudentiX boolean (body, required) - whether to include students
 *                                                  with a terminated career
 * @param adId           long (body, optional)    - ID (*) of the activity to
 *                                                  update segments for; if null,
 *                                                  all records are updated
 * @param adCod          string (body, optional)  - code (*) of the activity to
 *                                                  update segments for; if null,
 *                                                  all records are updated
 * @param aaOffId        long (body, optional)    - offer academic year to filter
 *                                                  activities; if null, all
 *                                                  records are updated
 * @return 201 Created on success, DettaglioErrore on failure
 *
 * (*) adId and adCod are mutually exclusive
 */
POST /aggcarr/SEG
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "filtriComuni": {
    "facId": 10, // Faculty/department ID (mutually exclusive with cdsId)
    "facCod": "facCod", // Faculty/department code (mutually exclusive with cdsCod)
    "cdsId": 10, // Degree course ID (mutually exclusive with facId)
    "cdsCod": "cdsCod", // Degree course code (mutually exclusive with facCod)
    "aaOrdId": 2010, // Curriculum academic year (requires cdsId or cdsCod)
    "pdsId": 9999, // Study plan ID (requires cdsId and aaOrdId)
    "pdsCod": "cdsCod", // Study plan code (requires cdsCod and aaOrdId)
    "aaRegId": 10 // Cohort year
  },
  "forzaADLibereFlg": false, // Recalculate segments for free activities (required)
  "forzaStatoFS": false, // Recalculate even if already attended/passed (required)
  "forzaStudentiX": false, // Include students with terminated career (required)
  "adId": 1, // Activity ID to update, null = all (*)
  "adCod": "adCod", // Activity code to update, null = all (*)
  "aaOffId": 2017 // Offer academic year filter, null = all
}
```

#### Response

**`201 Created`** - Procedure successfully triggered. Check the `link` response header for the preview URL.

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `PUT /aggcarr/SEG/{matId}` - Single execution: SEG

```java
/**
 * Triggers a single-student update of activity segments for student record
 * rows, syncing them from the reference offer.
 * The engine runs synchronously and returns the execution log directly.
 * filtriComuni is not required for single invocations.
 *
 * @param matId            long (path, required)    - ID of the student career
 *                                                    segment to process
 * @param forzaADLibereFlg boolean (body, required) - whether to recalculate
 *                                                    segments for free activities
 * @param forzaStatoFS     boolean (body, required) - whether to recalculate
 *                                                    segments even if the activity
 *                                                    is already attended or passed
 * @param forzaStudentiX   boolean (body, required) - whether to include students
 *                                                    with a terminated career
 * @param adId             long (body, optional)    - ID (*) of the activity to
 *                                                    update segments for; if null,
 *                                                    all records are updated
 * @param adCod            string (body, optional)  - code (*) of the activity to
 *                                                    update segments for; if null,
 *                                                    all records are updated
 * @param aaOffId          long (body, optional)    - offer academic year to filter
 *                                                    activities; if null, all
 *                                                    records are updated
 * @return List<LogAggiornaCarriera> execution log on success,
 *         DettaglioErrore on failure
 *
 * (*) adId and adCod are mutually exclusive
 */
PUT /aggcarr/SEG/{matId}
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "forzaADLibereFlg": false, // Recalculate segments for free activities (required)
  "forzaStatoFS": false, // Recalculate even if already attended/passed (required)
  "forzaStudentiX": false, // Include students with terminated career (required)
  "adId": 1, // Activity ID to update, null = all (*)
  "adCod": "adCod", // Activity code to update, null = all (*)
  "aaOffId": 2017 // Offer academic year filter, null = all
}
```

> `filtriComuni` is accepted but not required - `matId` in the path already scopes the execution to a single student.

#### Response

**`200 OK`** - Execution successful. Returns the execution log directly.

```json
[
  {
    "aggcarrId": 123, // Career update header ID
    "logTId": 123, // Log entry ID
    "logDes": "string" // Log description
  }
]
```

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `POST /aggcarr/SOST` - Bulk execution: SOST

```java
/**
 * Triggers a bulk substitution or insertion of a teaching activity in student
 * records. If adIdDaSostituire or adCodDaSostituire is null, the activity is
 * inserted as new rather than replacing an existing one.
 * On success, check the link response header for the URL of the generated
 * preview resource.
 *
 * @param filtriComuni       object (body, optional)  - common filters to scope
 *                                                      target students; facId or
 *                                                      cdsId required for bulk calls
 * @param forzaStatoS        boolean (body, required) - whether to include already
 *                                                      passed activities in the
 *                                                      substitution
 * @param adIdDaSostituire   long (body, optional)    - ID (*) of the activity to
 *                                                      replace; if null, a new
 *                                                      activity is inserted instead
 * @param adCodDaSostituire  string (body, optional)  - code (*) of the activity to
 *                                                      replace; if null, a new
 *                                                      activity is inserted instead
 * @param chiaveADdaInserire object (body, optional)  - key of the activity to
 *                                                      insert; provide IDs
 *                                                      (cdsId, aaOrdId, pdsId,
 *                                                      aaOffId, adId) or codes
 *                                                      (cdsCod, aaOrdId, pdsCod,
 *                                                      aaOffId, adCod)
 * @param udId               long (body, optional)    - ID of a specific teaching
 *                                                      unit to insert within the
 *                                                      activity
 * @return 201 Created on success, DettaglioErrore on failure
 *
 * (*) adIdDaSostituire and adCodDaSostituire are mutually exclusive
 */
POST /aggcarr/SOST
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "filtriComuni": {
    "facId": 10, // Faculty/department ID (mutually exclusive with cdsId)
    "facCod": "facCod", // Faculty/department code (mutually exclusive with cdsCod)
    "cdsId": 10, // Degree course ID (mutually exclusive with facId)
    "cdsCod": "cdsCod", // Degree course code (mutually exclusive with facCod)
    "aaOrdId": 2010, // Curriculum academic year (requires cdsId or cdsCod)
    "pdsId": 9999, // Study plan ID (requires cdsId and aaOrdId)
    "pdsCod": "cdsCod", // Study plan code (requires cdsCod and aaOrdId)
    "aaRegId": 10 // Cohort year
  },
  "forzaStatoS": false, // Include already passed activities (required)
  "adIdDaSostituire": 100, // Activity ID to replace, null = insert new (*)
  "adCodDaSostituire": "adCod", // Activity code to replace, null = insert new (*)
  "chiaveADdaInserire": {
    "cdsId": 1, // Degree course ID of the activity to insert
    "cdsCod": "CDS_AD_1", // Degree course code of the activity to insert
    "cdsDes": "string", // Degree course description
    "aaOrdId": 2016, // Curriculum academic year
    "aaOrdCod": "string", // Curriculum academic year code
    "aaOrdDes": "string", // Curriculum academic year description
    "pdsId": 1, // Study plan ID
    "pdsCod": "PDS_AD_1", // Study plan code
    "pdsDes": "string", // Study plan description
    "aaOffId": 1, // Offer academic year
    "adId": 1, // Teaching activity ID
    "adCod": "PDS_AD_1", // Teaching activity code
    "adDes": "string", // Teaching activity description
    "afId": 1 // U-Gov Didattica activity ID
  },
  "udId": 100 // Teaching unit ID to insert (optional)
}
```

#### Response

**`201 Created`** - Procedure successfully triggered. Check the `link` response header for the preview URL.

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

<br>

---

<br>

### `PUT /aggcarr/SOST/{matId}` - Single execution: SOST

```java
/**
 * Triggers a single-student substitution or insertion of a teaching activity
 * in the student record. If adIdDaSostituire or adCodDaSostituire is null,
 * the activity is inserted as new rather than replacing an existing one.
 * The engine runs synchronously and returns the execution log directly.
 * filtriComuni is not required for single invocations.
 *
 * @param matId              long (path, required)    - ID of the student career
 *                                                      segment to process
 * @param forzaStatoS        boolean (body, required) - whether to include already
 *                                                      passed activities in the
 *                                                      substitution
 * @param adIdDaSostituire   long (body, optional)    - ID (*) of the activity to
 *                                                      replace; if null, a new
 *                                                      activity is inserted instead
 * @param adCodDaSostituire  string (body, optional)  - code (*) of the activity to
 *                                                      replace; if null, a new
 *                                                      activity is inserted instead
 * @param chiaveADdaInserire object (body, optional)  - key of the activity to
 *                                                      insert; provide IDs
 *                                                      (cdsId, aaOrdId, pdsId,
 *                                                      aaOffId, adId) or codes
 *                                                      (cdsCod, aaOrdId, pdsCod,
 *                                                      aaOffId, adCod)
 * @param udId               long (body, optional)    - ID of a specific teaching
 *                                                      unit to insert within the
 *                                                      activity
 * @return List<LogAggiornaCarriera> execution log on success,
 *         DettaglioErrore on failure
 *
 * (*) adIdDaSostituire and adCodDaSostituire are mutually exclusive
 */
PUT /aggcarr/SOST/{matId}
```

**Auth:** `UTENTE_TECNICO` required · Supported: `Basic`, `JWT`

**Cache:** none

#### Request body

```json
{
  "forzaStatoS": false, // Include already passed activities (required)
  "adIdDaSostituire": 100, // Activity ID to replace, null = insert new (*)
  "adCodDaSostituire": "adCod", // Activity code to replace, null = insert new (*)
  "chiaveADdaInserire": {
    "cdsId": 1, // Degree course ID of the activity to insert
    "cdsCod": "CDS_AD_1", // Degree course code of the activity to insert
    "cdsDes": "string", // Degree course description
    "aaOrdId": 2016, // Curriculum academic year
    "aaOrdCod": "string", // Curriculum academic year code
    "aaOrdDes": "string", // Curriculum academic year description
    "pdsId": 1, // Study plan ID
    "pdsCod": "PDS_AD_1", // Study plan code
    "pdsDes": "string", // Study plan description
    "aaOffId": 1, // Offer academic year
    "adId": 1, // Teaching activity ID
    "adCod": "PDS_AD_1", // Teaching activity code
    "adDes": "string", // Teaching activity description
    "afId": 1 // U-Gov Didattica activity ID
  },
  "udId": 100 // Teaching unit ID to insert (optional)
}
```

> `filtriComuni` is accepted but not required - `matId` in the path already scopes the execution to a single student.

#### Response

**`200 OK`** - Execution successful. Returns the execution log directly.

```json
[
  {
    "aggcarrId": 123, // Career update header ID
    "logTId": 123, // Log entry ID
    "logDes": "string" // Log description
  }
]
```

**`422 Unprocessable Entity`** - Execution failed.

```json
{
  "statusCode": 200, // HTTP status code
  "retCode": -1, // Internal error code
  "retErrMsg": "string", // Error description
  "errDetails": [
    {
      "errorType": "string", // Error type (e.g. stackTrace)
      "value": "string", // Error detail
      "rawValue": "string" // Raw error detail (JSON)
    }
  ]
}
```

---

## References

- **Swagger UI:** [Aggcarr Api V1 - ESSE3 REST Docs](<https://unimol.esse3.cineca.it/e3rest/docs/?urls.primaryName=Aggcarr%20Api%20V1%20(https%3A%2F%2Funimol.esse3.cineca.it%2Fe3rest%2Fapi%2Faggcarr-service-v1)#/>)
- **Spec YAML:** [p11-aggcarrApiV1.yaml](https://unimol.esse3.cineca.it/e3rest/api/swagger-service-v1/swagger/specs/p11-aggcarrApiV1.yaml)
- **ESSE3 REST API General Documentation:** [wiki.u-gov.it](https://wiki.u-gov.it/confluence/display/ESSE3/Servizi+REST+su+ESSE3)
